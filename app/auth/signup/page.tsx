import React from "react";

const SignUpPage = () => {
    return (
        <div className="flex justify-center items-center h-screen font-space font-bold text-center">
            Only authenticated users are allowed to signup. <br /> Contact
            support for further information.
        </div>
    );
};

export default SignUpPage;
